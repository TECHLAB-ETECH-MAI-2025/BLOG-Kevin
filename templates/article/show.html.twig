{% extends 'base.html.twig' %}

{% block title %}{{ article.title }}{% endblock %}

{% block body %}
    <h1>Article</h1>
    {% if article.categories|length > 0 %}
        Catégories : {{ article.categories|map(c => c.name)|join(', ') }}
    {% endif %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ article.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ article.content }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex gap-2">
        <a href="{{ path('app_article_index') }}" class="btn btn-primary">back to list</a>

        <a href="{{ path('app_article_edit', {'id': article.id}) }}" class="btn btn-success" >edit</a>

        {{ include('article/_delete_form.html.twig') }}
    </div>
{% endblock %}
